
<section class="row">
			<div class="col-lg-4 col-md-4">
				<div class="row">
					<div class="col-lg-12 col-md-12">
						<div><h2>Burger to Eat</h2></div>
						<div >
              <ul>
              {{#each burger}}
                {{#unless devoured}}
                 <li>{{burger_name}}
                    <button id="button-devour" class="devour" data-id="{{id}}" data-devour="{{devoured}}">
                    Devour it!</button></li>
                {{/unless}}
              {{/each}}
            </ul>
            </div>
					</div>
				</div>
			</div>

			<div class="col-lg-4 col-md-4">
				<div class="row">
						<div class="col-lg-12 header-center">
        <div><img id="img-burger" src="../assets/images/burger-icon-black.png" alt="burger" /></div>
				<h1>EAT DA BURGER!</h1>
				<h3>If you can dream it, you can eat it!</h3>
					<h2>Create Your Burger</h2>
            <form class="create-form">

							<div class="col-lg-1 col-md-1"></div>
              <div class="form-group col-lg-10 col-md-10">
                <label for="burg">Burger Name:</label>
                <input class="form-control input-lg" type="text" id="burg" aria-describedby="Your Burger Name" name="name" />
                <button id="add-burger" class="btn-lg" type="submit">Add Burger</button>
              </div>
							<div class="col-lg-1 col-md-1"></div>

 
            </form>
					</div>
				</div>	
			</div>

			<div class="col-lg-4 col-md-4">
				<div class="row">
					<div class="col-lg-12 col-md-12">
						<div ><h2>Devoured Burgers</h2></div>
					    <ul id="pad-right">
                {{#each burger}}
                  {{#if devoured}}
                    <li>{{burger_name}}</li>
                  {{/if}}
                {{/each}}
              </ul>
					</div>
				</div>
			</div>
</section>
